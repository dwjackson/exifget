#include "libexifget.h"
#include <stdio.h>
    
void
print_ifd_entry(const struct exifget_ifd_entry *entry)
{
    char *tag_name;

    switch(entry->type) {
    default:
        printf("\n");
    }
}
